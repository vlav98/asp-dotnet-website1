@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@model MonPremierSite.Models.Client

<div>
    <div class="card">
        <div class="card-body">
            @ViewData["FirstName"]
            @ViewData["LastName"]
            @ViewData["Username"]
            @ViewData["Email"]
        </div>
    </div>
</div>
